# data files are local-only, never committed — except small manifests (fingerprints)
data/*
!data/manifests/

__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# screening の途中経過(公式 artifact は tier0_screening_*_v1.json)
experiments/phase7/*.checkpoint.jsonl
experiments/phase7/*_smoke.json
